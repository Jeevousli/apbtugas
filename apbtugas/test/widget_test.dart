import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — Firebase tidak dapat diinisialisasi di unit test
    // tanpa mocking. Implementasi integration test terpisah.
    expect(true, isTrue);
  });
}
